{
  "buildCommand": "cd web-kartavya && npm install --legacy-peer-deps && CI=false npm run build",
  "outputDirectory": "web-kartavya/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
